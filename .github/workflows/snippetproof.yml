name: SnippetProof
on: [push, pull_request]
jobs:
  snippetproof:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: xufei547/snippetproof@v1
        with:
          token: ${{ secrets.SNIPPETPROOF_TOKEN }}
